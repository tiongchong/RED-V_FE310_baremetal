# Examples

These examples are small reference snippets that reuse the same driver APIs as
the main CLI firmware.  They are not separate build targets yet; copy the
pattern into `src/main.c` or add a make target when you want a standalone app.
